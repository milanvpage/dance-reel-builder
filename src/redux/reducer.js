function reducer(state = {commercial: [], concert: [], theater: [], loading: false}, action){
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
}

export default reducer